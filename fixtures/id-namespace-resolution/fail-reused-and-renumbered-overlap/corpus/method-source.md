# Example Authority Document

## 3.2 Identifier Namespaces

| Prefix | Means | Prefix | Means |
| --- | --- | --- | --- |
| `FR-` | functional requirement | `AC-` | acceptance criterion |
| `SC-` | success criterion | `Q` | open question (Q1, Q2, ...) |

## Requirements

**FR-1** The gate reports every check that could not run as skipped, never as passed.

**FR-2** The system resolves every identifier to exactly one canonical definition.
