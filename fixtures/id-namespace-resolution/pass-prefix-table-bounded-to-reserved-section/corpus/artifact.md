# Example Candidate Artifact

This document cites **FR-1**, whose prefix the authority document declares once, inside its
own reserved section 3.2, with exactly the meaning the seeded reference carries. The two
other tables in that document — one in an earlier section, one inside a fenced example —
declare nothing, so there is no remeaning to report.
