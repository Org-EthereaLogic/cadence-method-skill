# Example Candidate Artifact

This document cites **FR-1**, which resolves and whose prefix is declared identically in
both documents' bound sections. It also mentions ZZ-9, a token whose prefix only the
reference's appendix decoy table carries: no reserved section declares ZZ-, so ZZ-9 is not
an identifier of this namespace and is neither resolved nor reported.
