# Example Candidate Artifact

This document cites **FR-1**, whose prefix the reference declares in a table written without
leading and trailing pipes. FR-1 is in scope only while those rows are read as declarations.
