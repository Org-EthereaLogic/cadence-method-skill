# Identifier Namespaces (trimmed section 3.2 excerpt)

This table is written without leading and trailing pipes, which GitHub-flavoured Markdown
allows. Each declaring row therefore BEGINS and ENDS with a backticked cell rather than with
a pipe, so a row parser that required an outer pipe, or that treated a row edged with
backticks as something other than a declaration, would drop every prefix this reference
declares and leave the artifact's FR-1 out of scope entirely.

Prefix | Means | Prefix
--- | --- | ---
`FR-` | functional requirement | `AC-`
`SC-` | success criterion | `Q`
