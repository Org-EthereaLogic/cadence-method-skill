# Identifier Namespaces — CADENCE Method Reference

This file carries two sections that each declare prefixes. The appendix is title-named and
its range is the SMALLER of the two, so smallest-range-wins alone would bind the namespace
to the retired legacy table and drop every prefix the artifact actually cites. Preferring a
number match over a title match is what selects the numbered section instead.

## Appendix: legacy identifier prefix table

| Prefix | Means |
| --- | --- |
| `LG-` | legacy prefix, retired |

## 3.2 Reserved prefixes

| Prefix | Means | Prefix | Means |
| --- | --- | --- | --- |
| `FR-` | functional requirement | `AC-` | acceptance criterion |
| `SC-` | success criterion | `Q` | open question (Q1, Q2, ...) |
