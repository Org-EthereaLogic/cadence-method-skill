# Identifier Namespaces (trimmed section 3.2 excerpt)

The first MEANING cell of the row below carries one unpaired backtick, exactly as a hurried
author leaves it. Under a masking rule that blanked any same-line inline code span merely
because the span contained a table delimiter, that stray backtick paired with the NEXT
column pair's prefix-cell backtick; the span between them held a delimiter, blanking it
deleted the declared acceptance-criterion cell, and the AC- prefix vanished from the
namespace. One character then turned the unresolvable AC-9 in the artifact into a silent
"skipped: not-applicable". The declaration below must survive the quirk.

| Prefix | Means | Prefix | Means |
| --- | --- | --- | --- |
| `FR-` | functional requirement (see `note) | `AC-` | acceptance criterion |
| `SC-` | success criterion | `Q` | open question (Q1, Q2, ...) |
