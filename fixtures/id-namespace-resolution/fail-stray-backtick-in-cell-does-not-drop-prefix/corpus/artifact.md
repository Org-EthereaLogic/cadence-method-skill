# Example Candidate Artifact

This document cites FR-1, which resolves, and AC-9, which no document defines. AC-9 is in
scope only while the reference's second column pair still declares the AC- prefix, so this
case fails exactly when that declaration survives the stray backtick sitting one cell to
its left.
