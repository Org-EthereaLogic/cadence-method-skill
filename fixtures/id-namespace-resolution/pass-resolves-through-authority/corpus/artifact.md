# Example Candidate Artifact

This document satisfies **FR-1** and **SC-1**, and reuses **AC-1** from the declared companion.

Constitution principles P1 through P7 govern this document's tone; they are not identifiers in this namespace.

A related script `AC-99.py` is referenced only as a file name, not as an identifier.

Its wording follows RFC-2119 keywords, stamps every time in ISO-8601, pins each artifact
by its SHA-256 digest, and is stored as UTF-8: the section 3.2 table reserves none of
those prefixes, so none of those tokens is an identifier of this namespace.
