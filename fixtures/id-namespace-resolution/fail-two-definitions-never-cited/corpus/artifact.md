# Example Candidate Artifact

This document cites **FR-1** only. It never cites the token the authority document
defines twice, so the doubled definition is caught in the definitions themselves and not
by way of any use here.
