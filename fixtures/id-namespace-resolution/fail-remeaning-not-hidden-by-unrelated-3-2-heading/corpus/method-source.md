# Example Authority Document

### 3.2 Non-functional requirements

This heading names section 3.2 of THIS document's own numbering, not method section 3.2,
and it declares no identifier prefixes at all. A bind that took the first heading matching
"3.2" would stop here, parse an empty table, conclude the authority document redeclares
nothing, and report a clean pass over the real remeaning below.

- The gate completes within the recorded budget.
- Every check is reproducible across two runs.

## Reserved identifier prefixes

| Prefix | Means | Prefix | Means |
| --- | --- | --- | --- |
| `FR-` | future request | `AC-` | acceptance criterion |
| `SC-` | success criterion | `Q` | open question (Q1, Q2, ...) |

## Requirements

**FR-1** The reserved FR- prefix is redeclared above with a meaning the seeded reference does not carry.
