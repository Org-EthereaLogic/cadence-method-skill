# Example Authority Document

## 3.2 Identifier Namespaces

| Prefix | Means | Prefix | Means |
| --- | --- | --- | --- |
| `FR-` | functional requirement | `AC-` | acceptance criterion |
| `SC-` | success criterion | `Q` | open question (Q1, Q2, ...) |

## Requirements

**FR-1** The system resolves every identifier to exactly one canonical definition.

## Declared companions

The only companion this document declares is notcompanion.md, whose basename merely
ends with the basename of the undeclared file below.

A worked example of what an admitting sentence would look like, shown as code and
therefore declaring nothing:

```markdown
Acceptance criteria for this document are recorded in companion.md.
```
