# Example Candidate Artifact

This document satisfies **FR-1** and cites **AC-1**, which exists only in a companion the
authority document never admits: its one prose mention is of a different file whose name
ends with that companion's basename, and its only literal mention sits inside a fenced
code block.
