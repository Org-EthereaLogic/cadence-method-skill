# Working Notes

A prefix table sits below, but this file declares no reserved-prefix section: no heading
here names method section 3.2, and no heading here names the prefix table either. Only a
heading counts, so this paragraph naming both does not make one.

| Prefix | Means | Prefix | Means |
| --- | --- | --- | --- |
| `FR-` | functional requirement | `AC-` | acceptance criterion |
| `SC-` | success criterion | `Q` | open question (Q1, Q2, ...) |

## Loose ends

Reading this table anyway would let any table anywhere in any file set the namespace
scope, so the check degrades closed instead of widening the parse to the whole document.
