# Example Companion (fixture excerpt)

## 4. Scope

Scope content for the companion document.

## 5. Assumptions

**AC-9.5** is the acceptance criterion this fixture's identifier citation
resolves against. Its prefix is the only identifier prefix defined anywhere
in this fixture's corpus, which is what makes the prose tokens UTF-8,
SHA-256, and GPT-4 unresolvable prefixes rather than dangling identifiers.

## Figure 2.1 — Zone flow

The dotted-label figure heading the artifact's citation resolves against.
