# Example Companion (fixture excerpt)

## 4. Scope

Scope content for the companion document.

## 5. Assumptions

**AC-9.5** is defined here so the identifier prefix "AC" is in use for this
fixture's corpus, which keeps the prose-token test honest: the artifact's
UTF-8, SHA-256, and GPT-4 are skipped for their own prefixes, not because
the corpus defines no identifiers at all.
