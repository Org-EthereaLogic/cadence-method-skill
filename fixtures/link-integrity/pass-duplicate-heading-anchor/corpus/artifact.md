# Pass Duplicate Heading Anchor

Two headings share the title "Details". GitHub disambiguates duplicate
heading ids by appending an incrementing suffix, so the SECOND "## Details"
is addressed by the anchor `#details-1`. Jump to the
[second details section](#details-1); it must resolve.

## Details

First details section, which owns the base anchor `#details`.

## Details

Second details section, addressed by `#details-1`.
