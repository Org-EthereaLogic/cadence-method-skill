# Warn Absolute Path Link

See the [legacy location](/etc/example/legacy-path.md) for the document as
it lived on a prior host. The path above is an inert test literal, in the
form of an absolute filesystem path, never resolved against this host.
