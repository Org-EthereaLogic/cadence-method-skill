# Symlink escape via link target

See [an ordinary in-corpus anchor](#symlink-escape-via-link-target) first, so
this case is not vacuous.

Then see the [escaping target](outside-root), which resolves through a
committed symlink that lives inside the checkout but points outside the
repository root.
