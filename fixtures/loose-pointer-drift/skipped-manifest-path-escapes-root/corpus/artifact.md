# Fixture Artifact — Never Read

This file exists only for fixture completeness. `manifest_path` in this
case's `input.json` escapes the repository root and is refused before this
artifact is ever opened.
