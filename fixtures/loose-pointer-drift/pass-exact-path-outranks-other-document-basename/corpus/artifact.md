# Fixture Artifact — Exact Path Outranks Another Document's Basename

The root-level `COMPANION_ONE.md` is at v1.4, which is exactly what the
manifest asserts for the document declaring that path. The deeper companion
that merely shares the basename is not named here, and attributing this
occurrence to it would report a drift no document has.
