# Fixture Artifact — Never Read

This file exists only for fixture completeness. `manifest_path` in this
case's `input.json` names a file that is deliberately not created, so the
check returns before this artifact is ever opened.
