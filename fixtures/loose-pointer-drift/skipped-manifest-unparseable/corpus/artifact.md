# Fixture Artifact — Never Read

This file exists only for fixture completeness. The manifest at
`corpus/manifest.json` is deliberately unparseable, so the check returns
before this artifact is ever opened.
