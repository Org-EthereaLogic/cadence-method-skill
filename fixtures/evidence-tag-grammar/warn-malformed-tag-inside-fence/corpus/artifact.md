# Discovery Notes — Fixture Artifact

The engagement scope was defined by the statement of work. (brief)

The platform's published SLA guarantees 99.9% uptime. (vendor documentation, verified 2026-01-10)

### A fenced tag example that is deliberately malformed

```text
Example of a tag missing its required date: (vendor documentation, verified)
```

### Ordinary fenced code and links, which are not evidence tags

```python
def fetch(timeout: int = 30):
    return requests.get("https://status.example.com", timeout=timeout)
```

```markdown
See the [status page](https://status.example.com) for details.
```

The client approved the revised timeline over email. (correspondence, screenshot-evidenced 2026-01-12)
